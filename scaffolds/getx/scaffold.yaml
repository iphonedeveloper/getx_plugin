name: dotquantum_getx
description: A scaffold with getx dependencies.
author: Pierpaolo Marturano <p.marturano@iphonedeveloper.it>

copy_files: true

structure:
  - ui:
    - common:
    - pages:
      - auth:
      - home:
      - themes:
  - data:
    - models:
    - providers:
    - repositories:
  - controllers:
    - auth:
    - home:
  - bindings:
  - routes:
  - services:
  - translations:
    - en_US:
    - it_IT:
  - utils:

dependencies: 
  google_fonts: ^2.3.2
  get: ^4.6.3
  firebase_core: ^1.17.0
  firebase_auth: ^3.3.18
  cloud_firestore: ^3.1.15
  firebase_storage: ^10.2.16
  cloud_functions: ^3.2.15
  flutter_form_builder: ^7.1.1
  json_annotation: ^4.5.0
  flutter_spinkit: ^5.1.0
  flutter_easyloading: ^3.0.3
  email_validator: ^2.0.1
  connectivity_plus: ^2.3.0
  image_picker: ^0.8.5+3
  image_cropper: ^2.0.2
  flutter_image_compress: ^1.1.0
  uuid: ^3.0.6
  cupertino_icons: ^1.0.4
  flutter_native_splash: ^2.2.0+1
   
dev_dependencies:
  